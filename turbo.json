{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["ALCHEMY_MAINNET_API_KEY"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
