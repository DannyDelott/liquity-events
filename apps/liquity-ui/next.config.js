module.exports = {
  // There is currently an issue causing the RainbowKit modal to stay open even
  // after clicking the "Close Modal" button in the top right. It currently only
  // affects development mode, however we should turn strict mode back on after
  // this is fixed: https://github.com/rainbow-me/rainbowkit/issues/837
  reactStrictMode: false,
};
